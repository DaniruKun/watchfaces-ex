<%= form_for @changeset, @action, fn f -> %>
  <%= if @changeset.action do %>
    <div class="alert alert-danger">
      <p>Oops, something went wrong! Please check the errors below.</p>
    </div>
  <% end %>

  <%= label f, :content %>
  <%= textarea f, :content %>
  <%= error_tag f, :content %>

  <%= label f, :author %>
  <%= number_input f, :author %>
  <%= error_tag f, :author %>

  <%= label f, :face %>
  <%= number_input f, :face %>
  <%= error_tag f, :face %>

  <div>
    <%= submit "Save" %>
  </div>
<% end %>
